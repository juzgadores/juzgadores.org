The command you should use to perform web searches is:

```bash
surfraw google "<Person Full Name>" -p | lynx -accept_all_cookies -verbose -dont_wrap_pre -display_charset=UTF-8 -dump -
```

1. Generate a search URL with Surfraw
2. Pass the URL to Lynx to get the search results text and links
